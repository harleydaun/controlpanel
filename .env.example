# Copy to .env (or set these in the Portainer stack "Environment variables" section)
IDRAC_HOST=192.168.1.120
IDRAC_USER=root
IDRAC_PASSWORD=calvin
# Preview the UI without touching real hardware:
# MOCK_IPMI=true
